# Bradbury deployment evidence

No deployment is recorded yet.

When deployment begins, record the repository commit, exact contract SHA-256, deployment transaction, contract address, Explorer link, and post-deployment source parity check together. Never reuse an Explorer link from an earlier deployment after the source changes.
